<div class="container m-3 d-flex">
  <img class="img-responsive center-block" src="{{user.image}}" alt="profile-image" style="width: 250px; margin-bottom: 25px">
  <div class="col-md-8 ml-5">
    <h3>{{user.name}}</h3>
    <p>{{user.email}}</p>
    <a href="/users/{{user.id}}/edit" class="btn btn-primary" role="button">Edit</a>
  </div>
</div>